import { SearchPetsPipe } from './search-pets.pipe';

describe('SearchPetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPetsPipe();
    expect(pipe).toBeTruthy();
  });
});
